export const STANDARD_WORK_CHECK_LABELS: Record<string, string> = {
  step_schema_present: "Every step has an action and a standard",
  metadata_present: "Version/owner/date are set",
  changed_steps_marked: "Changed steps are highlighted",
  steps_read_as_actions: "Steps read as actions, not policy",
};
