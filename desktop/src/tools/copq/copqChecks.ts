/** check_id -> plain-English label for T-02's prescore strip
 * (prescore/copq.py has two checks). */
export const COPQ_CHECK_LABELS: Record<string, string> = {
  total_matches_rows: "Total matches quantity x rate across rows",
  period_consistency: "Rows share one time period",
};
