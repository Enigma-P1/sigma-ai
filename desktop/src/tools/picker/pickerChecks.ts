/** check_id -> plain-English label for T-01's prescore strip
 * (prescore/picker.py only has one check today). */
export const PICKER_CHECK_LABELS: Record<string, string> = {
  routing_consistency: "Route matches your answers",
};
