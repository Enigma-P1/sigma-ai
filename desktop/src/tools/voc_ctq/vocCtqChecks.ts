/** check_id -> plain-English label for T-05's prescore strip
 * (prescore/voc_ctq.py has one check). */
export const VOC_CTQ_CHECK_LABELS: Record<string, string> = {
  tree_completeness: "Every need and CTQ resolves to a real statement/need",
};
